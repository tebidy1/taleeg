# مراجعة هندسية وتجربة مستخدم — منصة طيّار
## تحليل الوضع الحالي من الكود المصدري مباشرة + خارطة الطريق لتجربة عالمية

**المصدر:** قراءة مباشرة لـ `sessions.ts`، `prompt_builder.ts`, `gemini.ts`, `MissionActive.tsx`, `index.ts`, `mockData.ts` — وليس التقرير فقط.

---

## الخلاصة التنفيذية (30 ثانية)

البنية الأساسية سليمة الاتجاه: Gemini Live API ثنائي الاتجاه هو الخيار الصحيح للمحادثة الصوتية منخفضة التأخير مع طفل. لكن التنفيذ الحالي يضع **كامل ذكاء المنصة في برومبت واحد ضخم (~26 ألف حرف) يُرسل مرة واحدة ثم يُترك النموذج وحده يدير درساً من 7 دقائق** — بلا ساعة، بلا ذاكرة، بلا تحكم من الخادم. هذا هو السبب الجذري وراء "تجربة المدرب ليست أفضل": النماذج لا تستطيع تتبع الزمن، ولا الالتزام بسكريبت طويل، ولا التكيف بدون إشارات حية.

الحل ليس برومبت أفضل، بل **نقل عقل الدرس من البرومبت إلى الخادم** — والمفارقة أن 70% من الكود اللازم لذلك مكتوب عندكم بالفعل لكنه ميّت (غير مستدعى).

---

## أولاً: خريطة الوضع الحالي (كما هو في الكود فعلاً)

المسار: المتصفح ← WebSocket واحد ← باكند Node/Express ← WebSocket ثانٍ ← Gemini Live (`gemini-3.1-flash-live-preview`، صوت Charon، حرارة 0.8).

عند فتح الاتصال: يُبنى برومبت من 6 طبقات (شخصية أساسية + سكريبت مهمة + سيناريوهات متعددة + مراجعة شرطية + تكيف الطالب + قواعد لحظية + توجيه أخير) ويُرسل كـ `systemInstruction`، ثم رسالة `SESSION_START` نصية تجعل الكابتن يبدأ الكلام. بعدها الخادم مجرد أنبوب: صوت الطفل 16kHz صاعد، صوت النموذج 24kHz نازل، نصوص التفريغ في الاتجاهين، وأداة `yield_turn` لإنهاء دور النموذج.

الإنهاء: زر يدوي فقط، بنقاط ثابتة (120) وشارة ثابتة. لا قاعدة بيانات — كل شيء mock.

---

## ثانياً: النتائج مرتبة بالخطورة

### 🔴 حرج — يفسد جوهر التجربة

**1. منطق التكيف الحي كله كود ميّت.**
`buildRealTimeUpdate()`, `generateRealTimeRules()` (كشف الإحباط/القلق/التدفق، تبديل الأوضاع)، `detectCurrentPhase()` — كلها مكتوبة بعناية في `prompt_builder.ts` **ولا يستدعيها أي سطر في `sessions.ts`**. لا يوجد `setInterval` ولا أي محرك حالة. النموذج يُخبَر عند الثانية صفر أن الوقت "00:00" ولا يُحدَّث أبداً. النتيجة الحتمية: الدرس ينحرف، يطول، يقصر، ولا يتكيف — وهذا بالضبط ما لاحظتموه في تجربة المدرب.

**2. لا توجد ذاكرة إطلاقاً — والذاكرة هي "الخلطة السحرية" رقم 1.**
البرومبت يطلب من النموذج إخراج "JSON assessment" في نهاية الجلسة، لكن: (أ) النموذج في وضع AUDIO فقط فلا قناة نصية لالتقاطه، (ب) لا يوجد أي كود يلتقطه أو يخزنه، (ج) الطالب mock ثابت (`Faisal`، صفر جلسات) في كل جلسة. إذن الكابتن لن يتذكر اسم قطة الطفل غداً أبداً — الحلقة اليومية التي يقوم عليها المنتج غير موجودة تقنياً. **المفارقة: الخادم يمرّ عليه كل التفريغ النصي أصلاً (سطر 141-158 في sessions.ts) ويكتفي بتمريره — تخزينه يكلف سطرين.**

**3. إعدادات VAD معادية للأطفال.**
`silenceDurationMs: 300` + حساسية نهاية كلام HIGH (gemini.ts:41-47). الطفل المبتدئ يتلعثم: "I... want... umm...". بعد 300ms صمت يقطعه النموذج ويرد. هذا أسرع مقاطعة ممكنة في الإعدادات — مناسب لبالغ يتحدث لغته الأم، وكارثي على طفل A1 يحتاج 1-2 ثانية ليركّب الجملة. هذا وحده قد يفسر معظم شعور "التجربة غير المضبوطة".

**4. الطفل لا يستطيع مقاطعة الكابتن (half-duplex قسري).**
`if (isSpeakingRef.current) return;` (MissionActive.tsx:338) يكتم المايك كلياً أثناء صوت الكابتن. إذا أطال النموذج الكلام (وسيُطيل مع برومبت 26 ألف حرف) يجلس الطفل ينتظر بلا حيلة. الأسوأ: معالج `interrupted` موجود في الكود لكنه لا يمكن أن يُطلَق من كلام الطفل لأن صوته لا يُرسل أصلاً أثناء كلام النموذج. و`echoCancellation: true` مفعّل بالفعل — الكتم طبقة ثانية خشنة تلغي أهم ميزة في Live API: المقاطعة الطبيعية.

**5. ثلاثة أنظمة مراحل متناقضة تعمل في وقت واحد.**
- البرومبت: مراحل بالثواني (flash_open حتى 15s... victory_close حتى 7:00) — والنموذج لا يملك ساعة.
- `detectCurrentPhase()` بالثواني — كود ميت.
- الواجهة: `getPhase(completedExchanges)` بعدد التبادلات (MissionActive.tsx:67-73) — والعدّ نفسه غير موثوق لأن `inputTranscription` قد يصل مجزأً فيُحتسب التبادل الواحد مرات.
كما يتناقض حد الإنهاء: "7:30" في Layer 4 المُرسل و"5:00" في النسخة الميتة. العنوان الذي يراه الطفل ("إحماء/مهمة/تطبيقات") قد لا يطابق ما يفعله الكابتن فعلاً.

**6. مؤشرات مزيفة أمام الطفل.**
`pronunciationScore` ثابت 75 (سطر 51)، النقاط ثابتة 120، الشارة ثابتة "First Flight". الأطفال يكتشفون المكافآت الزائفة خلال 3 جلسات — وعندها تنهار مصداقية نظام المكافآت كله (وهو ثاني أهم محرك عودة).

### 🟠 عالي — أداء وموثوقية

**7. برومبت 26 ألف حرف يعمل ضدكم.**
تكلفة أعلى لكل جلسة، زمن أول استجابة أطول، والأهم: التخفيف (dilution) — النماذج تعطي وزناً للبداية والنهاية وتُضعف الوسط، لذلك تُهمَل القواعد الحرجة المدفونة (وأنتم أضفتم `generateFinalDirective` كترقيع لهذه المشكلة تحديداً). القاعدة العملية: تعليمات النظام في محادثة صوتية حية يجب ألا تتجاوز 3-5 آلاف حرف، والباقي يُحقن تدريجياً وقت الحاجة.

**8. زمن الوصول لأول صوت غير مُدار.**
السلسلة تسلسلية بالكامل: فتح الصفحة ← WS ← بناء برومبت ← await اتصال Gemini ← تريغر ← توليد ← أول صوت. طوالها يحدّق الطفل في "جاري الاتصال...". لا يوجد pre-warming ولا صوت ترحيبي محلي فوري.

**9. أنبوب الصوت هش.**
- `ScriptProcessorNode` مهجور رسمياً (يعمل على الخيط الرئيسي = تقطيع) — البديل `AudioWorklet`.
- `downsample()` تقطيع عينات ساذج بدون low-pass filter = تشويه aliasing يُضعف دقة التعرف على كلام طفل أصلاً صعب التعرف عليه.
- التشغيل بسلسلة `onended` بدون جدولة بـ `ctx.currentTime` = فجوات ونقرات بين المقاطع.
- حالة `speaking→listening` تُقلب عند فراغ الطابور مؤقتاً ولو كان النموذج ما زال في منتصف دوره → يفتح المايك لحظياً ويلتقط صدى.
- الصوت النازل base64 داخل JSON (+33% حجماً) بينما الصاعد binary — ازدواجية بلا مبرر.

**10. لا إنهاء مفروض من الخادم = تسريب مال وتجربة.**
لا مؤقت يغلق الجلسة. جلسة منسية تُبقي اتصال Gemini Live مفتوحاً يحاسبكم عليه. والدرس الذي لا ينتهي في القمة (قانون الذروة-النهاية) يقتل الشوق للغد. الحدود موثقة في البرومبت (7:30) لكن لا شيء يفرضها.

**11. وضع الديمو الصامت يخفي الأعطال.**
عند فشل الاتصال تظهر رسالة ترحيب نصية مزيفة بلا صوت وبمايك لا يفعل شيئاً — الطفل يظن التطبيق يعمل وهو معطل. الفشل يجب أن يكون صريحاً ولطيفاً ("الكابتن يجهز الطائرة! لحظة...") مع إعادة محاولة تلقائية.

**12. أمان صفري على قناة الجلسة.**
`ws://localhost` ثابت في الكود، sessionId يولّده العميل بلا تحقق، لا أصل (origin) يُفحص، لا رمز جلسة، مسار `POST /api/sessions/start` موجود ولا يُستخدم. مقبول في MVP محلي، قاتل عند أول نشر.

### 🟡 متوسط

**13. `yield_turn` بلا شبكة أمان:** إن نسي النموذج استدعاءها لا يوجد fallback خادمي (مؤقت يقطع الدور).
**14. توجيه زر المساعدة يُحقن كدور "user"** وقد يتزامن مع كلام الطفل الفعلي فيشوش الدور.
**15. Supabase مثبتة ولا تُستخدم؛ zustand مثبتة والحالة كلها useState محلي؛ srs.ts يقرأ مفردات mock فارغة** — بنية تحتية اشتُريت ولم تُركّب.

---

## ثالثاً: المعمارية المستهدفة — "الخادم يقود، النموذج يمثّل"

المقارنة بين الخيارات الثلاثة الممكنة:

| | (أ) الحالي: برومبت ضخم + Live | (ب) **الموصى به: Live + مايسترو خادمي** | (ج) بايبلاين STT→LLM→TTS |
|---|---|---|---|
| تحكم في إيقاع الدرس | لا شيء | كامل (ساعة + حالة على الخادم) | كامل |
| زمن الاستجابة | ممتاز (<1s) | ممتاز (<1s) | ضعيف (1.5-3s) — قاتل للأطفال |
| مقاطعة طبيعية | ممكنة (معطلة حالياً) | نعم | صعبة جداً |
| التكلفة/جلسة | مرتفعة (برومبت ضخم + ثرثرة نموذج) | **أقل** (برومبت صغير + كلام نموذج مقنن) | الأقل نظرياً لكن بجودة تجربة أدنى |
| موثوقية السكريبت | منخفضة | عالية | عالية |

**الخيار (ب) بالتفصيل — وهو إعادة توصيل أكثر منه إعادة كتابة:**

1. **مايسترو الجلسة (Session Orchestrator) في الباكند:** كائن لكل جلسة يملك: ساعة حقيقية، عدّاد تبادلات (من أحداث `turnComplete` و`inputTranscription` التي تمر عليه أصلاً)، وحالة المرحلة عبر `detectCurrentPhase()` **الموجودة**. عند كل انتقال مرحلة أو كل 30 ثانية يحقن توجيهاً قصيراً عبر `sendClientContent` (نفس آلية زر المساعدة العاملة اليوم) باستخدام `buildRealTimeUpdate()` **الموجودة**. كشف المزاج مبدئياً بقواعد بسيطة من التفريغ (طول الردود، التردد، كلمات عربية استغاثة) — يكفي لتفعيل أوضاع COMFORT/CHALLENGE **المكتوبة**.

2. **تقليص systemInstruction إلى ≤5K حرف:** شخصية + قواعد أمان الطفل + قواعد الدور والإيجاز + وصف موجز للمهمة. خطوات السكريبت تُحقن مرحلةً مرحلة من المايسترو، فلا يستطيع النموذج "حرق" الدرس ولا نسيان قاعدة مدفونة. (أثر جانبي مباشر: خفض تكلفة كل جلسة وتسريع أول رد.)

3. **حلقة الذاكرة (الأولوية القصوى منتجياً):**
   - الخادم يخزّن التفريغ الكامل + أحداث الجلسة (يمرّ عليه كله اليوم بالفعل).
   - بعد إغلاق الجلسة: استدعاء **نموذج نصي رخيص** (Flash عادي، ليس Live) يستخرج: ملخص الجلسة، حقائق شخصية جديدة عن الطفل (قطته، لعبته المفضلة)، تقييم الأنماط والمفردات لتحديث SRS، مرشح "أفضل لحظة" لمقطع الأهل، وبذرة كليفهانجر الغد.
   - يُكتب كله في Supabase (المثبتة أصلاً) ويتغذى منه `generateLayer3()` **الموجود** في الجلسة التالية.
   - هذه الخطوة وحدها تحول المنتج من "لعبة تجريبية" إلى "صديق يتذكرني" — وتكلفتها استدعاء نصي واحد رخيص لكل جلسة.

4. **إصلاح طبقة الصوت للأطفال:**
   - `silenceDurationMs: 1000-1200` وحساسية نهاية متوسطة/منخفضة، مع خفض تدريجي حسب مستوى الطالب.
   - إزالة كتم المايك والاعتماد على AEC + إشارة `interrupted` (المعالَجة أصلاً في الطرفين) = مقاطعة طبيعية.
   - `AudioWorklet` بدل ScriptProcessor، ومرشح low-pass قبل خفض العينة، وجدولة تشغيل بـ `ctx.currentTime` مع مخزن jitter صغير (~150ms).
   - الصوت النازل binary مثل الصاعد.

5. **إنهاء مفروض خادمياً:** حقن توجيه "victory close" عند 6:30، إنهاء قسري لطيف عند 8:00، إغلاق جلسة Gemini دائماً في `finally`. يحمي التكلفة ويصنع نهاية-الذروة كل يوم.

6. **صفر انتظار محسوس:** فتح WS وبناء البرومبت والاتصال بـ Gemini **أثناء** شاشة الانطلاق/العد التنازلي في الواجهة، مع صوت ترحيبي محلي مسجّل يُشغَّل فوراً ("!Captain English is starting the engines") — أول صوت حقيقي يصل قبل أن ينتهي.

7. **صدق المؤشرات:** ربط النقاط بعدّادات المايسترو الحقيقية (تبادلات ناجحة، استخدام تلميحات، إتمام مراحل)، حذف درجة النطق حتى تُحسب فعلاً، وأحداث مرحلة من الخادم تقود شريط التقدم بدل عدّ التخمين في الواجهة.

8. **قياس كل شيء:** لكل جلسة سجّلوا: زمن أول صوت، نسبة كلام الطفل، عدد الأدوار، نقطة الانسحاب، الإتمام. بدون هذه الأرقام لن تعرفوا أبداً إن كانت "الخلطة" تتحسن. (تتصل مباشرة بلوحة القياس في وثيقة التجربة السابقة.)

---

## رابعاً: خطة تنفيذ واقعية

**الأسبوع 1 — إصلاحات لا تحتاج معمارية (أثر فوري على التجربة):**
VAD إلى 1000ms/حساسية متوسطة • إزالة كتم المايك • إصلاح قلب speaking/listening (الاعتماد على turn_complete لا فراغ الطابور) • مؤقت إنهاء خادمي 8 دقائق + إغلاق Gemini المضمون • حذف درجة النطق المزيفة • تخزين التفريغ في ملف/جدول.

**الأسابيع 2-3 — المايسترو:**
تفعيل الكود الميت: حالة جلسة خادمية + `detectCurrentPhase` + حقن `buildRealTimeUpdate` عند تغير المرحلة • تقليص systemInstruction وحقن السكريبت مرحلياً • ربط أحداث المرحلة بالواجهة • fallback لـ `yield_turn`.

**الأسابيع 3-4 — الذاكرة (أكبر قفزة منتجية):**
Supabase: جداول طالب/جلسة/ذاكرة • استدعاء التلخيص النصي بعد كل جلسة • تغذية Layer 3 من بيانات حقيقية • تفعيل SRS الحقيقي • أول نسخة من "استدعاء الأمس" في افتتاحية الدرس.

**الأسبوع 5 — الصقل:**
AudioWorklet + جدولة دقيقة • pre-warming وصوت ترحيبي فوري • WSS + رمز جلسة من `/start` الموجود • لوحة مقاييس الجلسات.

---

## خامساً: رافعات التكلفة (بالترتيب الفعلي للأثر)

1. **تقنين كلام النموذج** — كل ثانية يتكلمها الكابتن توكنز صوتية مدفوعة؛ فرض قاعدة ≤30% عبر المايسترو يخفض التكلفة ويحسّن التعلم معاً (نادراً ما يتحالف العاملان هكذا).
2. **تقليص البرومبت من 26K إلى 5K** — يُدفع مع كل جلسة.
3. **سقف الجلسة المفروض** — لا جلسات معلقة تُحاسَبون عليها.
4. **التقييم خارج الجلسة الحية** بنموذج نصي رخيص بدل مطالبة نموذج الصوت الغالي بإخراج JSON (لا يعمل أصلاً حالياً).
5. عند التوسع لاحقاً: فحص أسعار Live API وقتها وتقييم وضع "اقتصادي" بجودة صوت أقل للأسواق الحساسة سعرياً — لا تقرروا هذا الآن.

---

## الخلاصة

المشكلة ليست في اختيار Gemini Live ولا في جودة كتابة البرومبتات — كلاهما جيد. المشكلة أن **الدرس بلا مايسترو**: لا ساعة، لا حالة، لا ذاكرة، ومايك مكتوم نصف الوقت وVAD يقطع الطفل بعد 300ms. عالجوا هذه الخمسة (وأغلب عدّتها مكتوبة عندكم بالفعل كدالّات ميتة) وستقفز التجربة من "ديمو ذكي" إلى شيء يستحق أن يدمنه طفل.
